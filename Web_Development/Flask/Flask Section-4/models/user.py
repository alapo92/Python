from db import db


class UserModel(db.Model):
    __tablename__ = 'users'
    id = db.Column(db.Integer, primary_key=True)
    username = db.Column(db.String(80))
    password = db.Column(db.String(80))

    def __init__(self, username, password):
        self.username = username
        self.password = password

    def save_to_db(self):
        db.session.add(self)
        db.session.commit()

    @classmethod
    def find_by_username(cls, username):
        return cls.query.filter_by(username=username).first()

    @classmethod
    def find_by_id(cls, _id):
        return cls.query.filter_by(id=_id).first()

#
# This is an API (not a rest api). The API exposes two endpoints
# those two methods are an interface for the rest of the program to interact with the user
# like writing or retrieving it from the DB. As long as the API remains unchanged,
# There is no need to worry about the impact of any further changes to the rest of the program
#
